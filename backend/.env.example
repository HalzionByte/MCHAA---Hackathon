# Application Server Configuration
PORT=8000
HOST=0.0.0.0

# Database Configuration (Defaults to SQLite crop_health.db if not provided)
DATABASE_URL=sqlite:///./crop_health.db

# AI & LLM Service Keys (Optional for local development; system defaults to mock diagnosis if unconfigured)
GEMINI_API_KEY=your_google_gemini_api_key_here
CLAUDE_API_KEY=your_anthropic_claude_api_key_here
